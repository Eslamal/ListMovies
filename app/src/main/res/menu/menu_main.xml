<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <!-- Search action with custom icon -->
    <item
        android:id="@+id/action_search"
        android:icon="@android:drawable/ic_search_category_default"
        android:title="@string/search"
        android:contentDescription="@string/search"
        app:showAsAction="always"
        app:iconTint="@android:color/white" />


    <!-- Favorites -->
    <item
        android:id="@+id/action_favorites"
        android:icon="@drawable/ic_favorite"
        android:title="@string/favorites_title"
        android:contentDescription="@string/favorites_title"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_watchlist"
        android:icon="@drawable/ic_bookmark"
        android:title="@string/watchlist_title"
        android:contentDescription="View Watchlist"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/ic_settings" android:title="@string/settings"
        app:showAsAction="never" />




</menu>

